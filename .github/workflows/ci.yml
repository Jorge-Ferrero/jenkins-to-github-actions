name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup build environment
        run: echo "Setting up build environment"

      - name: Build
        run: echo "Running build step (simulated)"

      - name: Test
        run: echo "Running test step (simulated)"
